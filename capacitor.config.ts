import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sescmaster.app',
  appName: 'sescmaster',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
